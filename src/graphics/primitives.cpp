#include <graphics/primitives.h>

void GenerateLines(vector3d<glm::vec3>& data)
{
    glm::uvec3 data_size(500, 500, 100);
    glm::vec3 grid_size = {1.f, 1.f, 1.f};



    for (int x = 0; x < data_size.x; x++)
    {
        for (size_t y = 0; y < data_size.y; y++)
        {
            for (size_t z = 0; z < data_size.z; z++)
            {
                
            }
            
        }
        
    }
    
}